spring:
  datasource:
    url: jdbc:mysql://localhost:3306/family_task_manager
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none # Use 'update' or 'validate' for development, 'none' for production
    show-sql: true
